/*********************************************************************************************************
* 模块名称：RCU.c
* 摘    要：RCU模块
* 当前版本：1.0.0
* 作    者：Leyutek
* 完成日期：2021年07月01日 
* 注    意：本文件原包含系统时钟配置代码（针对F303 120MHz），但与F310（72MHz）不兼容且导致启动超时。
*           系统时钟配置已移交给 system_gd32f3x0.c 中的 SystemInit() 处理。
*           保留此文件以维持工程结构，但 InitRCU 为空函数。
*********************************************************************************************************/

#include "RCU.h"
#include "gd32f3x0_conf.h"

/*********************************************************************************************************
* 函数名称：InitRCU
* 函数功能：初始化RCU模块  
* 输入参数：void
* 输出参数：void
* 返 回 值：void
* 创建日期：2021年07月01日
* 注    意：为空函数
*********************************************************************************************************/
void InitRCU(void)
{
  /* 
   * 系统时钟已在 SystemInit() 中配置为 72MHz (IRC8M + PLL)。
   * 此处无需再次配置，且原有的 HXTAL 配置会导致 F310 启动超时（因无外部晶振）。
   */
}
